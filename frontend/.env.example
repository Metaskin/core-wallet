# Backend API base URL.
# In development this is NOT needed — Vite proxies /api → localhost:5001 automatically.
# In production (AWS Amplify), set this to your Elastic Beanstalk / EC2 backend URL.
VITE_API_URL=https://your-backend-domain.com/api
